Changelog:

- Cleaned code until line 224
- Fixed a bug in orthogonal cameras where the left value was being read 2 times
- Fixed a bug where the right value was not being read
- changed the way cameras are read, so that it reads all cameras instead of only one of each type